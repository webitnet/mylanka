# No build-time env required for the Mini App itself — all backend calls
# go through the rewrite to https://mylanka.com.ua/api/* configured in
# vercel.json. Keep this file as a placeholder in case future builds need
# Vite envs (VITE_*).
